# --- PAI App Configuration ---
ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
PRINCIPAL_NAME=User
TIMEZONE=UTC
AI_NAME=PAI
CATCHPHRASE=Ready to go
TEMPERATURE_UNIT=fahrenheit
ELEVENLABS_VOICE_ID=pNInz6obpgDQGcFmaJgB

# --- SSH Configuration ---
# PAI_PASSWORD: Required for SSH login (user: pai)
PAI_PASSWORD=
# SSH_PORT: External port for SSH access
SSH_PORT=2222

# --- Network & Security Configuration ---
# BIND_IP: Default is 127.0.0.1 (Loopback). Set to 0.0.0.0 for public or your VPN IP.
BIND_IP=127.0.0.1
BIND_PORT=8082
VOICE_PORT=8888
# FQDN: Your domain name (e.g., pai.yourdomain.com)
FQDN=localhost
